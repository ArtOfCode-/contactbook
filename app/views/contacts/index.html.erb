<h1>Listing Contacts</h1>

<table>
  <thead>
    <tr>
      <th>First</th>
      <th>Last</th>
      <th>Title</th>
      <th>City</th>
      <th>Phone</th>
      <th>Email</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @contacts.each do |contact| %>
      <tr>
        <td><%= truncate(contact.first, 20) %></td>
        <td><%= truncate(contact.last, 20) %></td>
        <td><%= truncate(contact.title, 20) %></td>
        <td><%= truncate(contact.city, 20) %></td>
        <td><%= truncate(contact.phone, 20) %></td>
        <td><%= truncate(contact.email, 20) %></td>
        <td><%= link_to 'Show', contact %></td>
        <td><%= link_to 'Edit', edit_contact_path(contact) %></td>
        <td><%= link_to 'Delete', contact, :method => :delete, data: { :confirm => 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Contact', new_contact_path %> |
<a href="/exports/json">Export all as JSON</a> |
<a href="/exports/xml">Export all as XML</a>
