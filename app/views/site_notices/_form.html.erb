<%= form_for(@site_notice) do |f| %>
  <% if @site_notice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@site_notice.errors.count, "error") %> prevented this notice from being saved:</h2>
      <ul>
        <% @site_notice.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
